# data — the big raw CSV and the built DB are never committed
data/*.csv
data/*.csv.gz
data/*.duckdb
data/*.duckdb.wal

# build outputs
exports/*.parquet
reports/figures/*.png
reports/powerbi_dashboard.pdf
artifacts/

# EXCEPTIONS: small artefacts the hosted Streamlit app needs are committed on purpose.
# Re-enable model.joblib / metrics.json once they are trained on the REAL dataset —
# a synthetic-trained model must not be committed.
!exports/simulator_base_sample.parquet
# !artifacts/model.joblib
# !artifacts/metrics.json

# demo/synthetic data generator output (keep the script, not its output)
data/synthetic_accepted.csv

# python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# power bi
*.pbix

# os
.DS_Store
