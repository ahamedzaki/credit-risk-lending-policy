# Deployed Streamlit app (app/simulator.py) — kept minimal so Streamlit Community Cloud
# builds fast. For the full pipeline use requirements-dev.txt.
# Python 3.11.
streamlit==1.40.1
pandas==2.2.3
numpy==2.1.3
pyarrow==18.0.0
PyYAML==6.0.2
